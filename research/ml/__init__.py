"""research.ml — Machine learning components for IteraDynamics.

Currently contains:
  calibration  — Post-process heuristic confidence scores → calibrated P(win)
  regime_clf   — Placeholder for future ML regime classifier
"""
