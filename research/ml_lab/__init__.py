"""Exploratory ML infrastructure; never an operational strategy dependency.

Versioned definitions preserve recorded experiment semantics. New research must
select its definition explicitly rather than changing a historical version.
"""
